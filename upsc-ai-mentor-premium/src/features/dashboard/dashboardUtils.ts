import type { ActivityEvent } from '../../api/types'
export const clampPercent=(value:number)=>Number.isFinite(value)?Math.max(0,Math.min(100,value)):0
export const formatDuration=(seconds:number)=>{const minutes=Math.max(0,Math.round((Number.isFinite(seconds)?seconds:0)/60));return minutes<60?`${minutes} min`:`${Math.floor(minutes/60)} hr${minutes>=120?'s':''}${minutes%60?` ${minutes%60} min`:''}`}
export function weeklyStudy(events:ActivityEvent[]){const days=Array.from({length:7},(_,offset)=>{const date=new Date();date.setHours(0,0,0,0);date.setDate(date.getDate()-(6-offset));return {key:date.toLocaleDateString('en-CA'),label:date.toLocaleDateString(undefined,{weekday:'short'}),seconds:0}});for(const event of events){if(event.event_type!=='study_time_logged'||!event.duration_seconds)continue;const key=new Date(event.occurred_at).toLocaleDateString('en-CA');const day=days.find(item=>item.key===key);if(day)day.seconds+=event.duration_seconds}return days}
export const relativeTime=(value:string)=>{const seconds=Math.max(0,Math.round((Date.now()-new Date(value).getTime())/1000));if(seconds<60)return 'Just now';if(seconds<3600)return `${Math.floor(seconds/60)}m ago`;if(seconds<86400)return `${Math.floor(seconds/3600)}h ago`;return `${Math.floor(seconds/86400)}d ago`}
